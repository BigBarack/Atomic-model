import numpy as np
import matplotlib.pyplot as plt
import matplotlib.animation as animation

n=100

#starting a collocated grid(right now i just want visuals to work)

Ex_field = np.zeros((n,n,n))
Ey_field = np.zeros((n,n,n))
Ez_field = np.zeros((n,n,n))

Hx_field = np.zeros((n,n,n))
Hy_field = np.zeros((n,n,n))
Hz_field = np.zeros((n,n,n))



efe
def i
